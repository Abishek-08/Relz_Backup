syntax = "proto3";

package detect;

message TestRequest {
    string msg = 1;
}

message TestResponse {
    string msg = 1;
}

service DetectService {
    rpc TestMethod(TestRequest) returns (TestResponse);
}