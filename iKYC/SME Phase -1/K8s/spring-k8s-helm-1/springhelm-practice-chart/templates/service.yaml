apiVersion: v1
kind: Service
metadata:
  name: {{ include "springhelm-practice-chart.fullname" . }}
  labels:
    {{- include "springhelm-practice-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "springhelm-practice-chart.selectorLabels" . | nindent 4 }}
