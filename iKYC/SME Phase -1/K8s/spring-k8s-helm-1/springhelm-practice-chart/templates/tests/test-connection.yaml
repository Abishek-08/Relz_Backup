apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "springhelm-practice-chart.fullname" . }}-test-connection"
  labels:
    {{- include "springhelm-practice-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "springhelm-practice-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
