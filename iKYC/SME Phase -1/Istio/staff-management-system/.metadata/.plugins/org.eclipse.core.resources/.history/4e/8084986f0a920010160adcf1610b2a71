package com.staff.client;

import org.springframework.cloud.openfeign.FeignClient;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;

import com.staff.dto.StaffFindDTO;

@FeignClient(name="department-service", url="http://localhost:8024")
public interface DepartmentClient {
	
	@GetMapping("/department/get/staff/{staffId}")
	StaffFindDTO getStaffByDepartment(@PathVariable("staffId") int staffId);

}
