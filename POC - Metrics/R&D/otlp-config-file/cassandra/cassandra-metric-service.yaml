apiVersion: v1
kind: Service
metadata:
  name: cassandra-metrics
  namespace: database
spec:
  selector:
    app: cassandra
  ports:
    - name: metrics
      port: 5556
      targetPort: 5556
  clusterIP: None
